{
  "name": "push-guard",
  "owner": {
    "name": "kawaz"
  },
  "metadata": {
    "description": "Block direct git/jj push and route through repo task runner (just push)",
    "version": "0.3.1"
  },
  "plugins": [
    {
      "name": "push-guard",
      "description": "PreToolUse(Bash) hook that blocks `git push` / `jj git push` so Claude must go through repo-defined `just push` — keeps check/test/version-bump/translation gates from being bypassed.",
      "source": "./"
    }
  ]
}
